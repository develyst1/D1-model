{
  "name": "d1-model-backend",
  "version": "0.1.0",
  "description": "D1-Model robot-brain Backend API — conversation history (PostgreSQL CRUD) + AI Develyst gateway proxy",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
