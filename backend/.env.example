# PostgreSQL connection
DATABASE_URL=postgres://dev_user:smart2026@localhost:5432/d1_model_db
DATABASE_SSL=false

# AI Develyst gateway (see H:\chipint\develyst-ai)
AI_GATEWAY_URL=http://localhost:3009
# ใส่ provider/model เพื่อบังคับใช้ค่าเริ่มต้น ถ้าเว้นว่างจะปล่อยให้ gateway fallback เอง
AI_DEFAULT_PROVIDER=
AI_DEFAULT_MODEL=

PORT=3100
